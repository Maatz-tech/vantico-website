<section class="remote">
  <div class="remote__container">
    <div class="remote__image-wrap">
      <img src="/images/carreira/remote-map.png" alt="" class="remote__image-inner" loading="lazy" />
    </div>
    <div class="remote__content" data-animate>
      <h2 class="remote__title" data-animate data-animate-type="typing">Em qualquer lugar</h2>
      <p class="remote__text">Aqui entendemos que a confiança e a flexibilidade andam juntas para criar uma rotina produtiva e saudável. Por isso, nossos especialistas estão espalhados <strong>por todo Brasil e trabalham de forma 100% remota.</strong> Esse modelo de trabalho nos permite reunir pessoas altamente qualificadas, independentemente de localização. Com processos bem definidos e uma comunicação próxima, garantimos colaboração eficiente e resultados consistentes em todos os projetos.</p>
      <a href="https://vantico.com.br/vagas" class="remote__cta">
        Ver vagas
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      </a>
    </div>
  </div>
</section>
<style>
  .remote { background-color: #ffffff; padding: 48px 0; }
  .remote__container {
    max-width: var(--container-max); margin: 0 auto;
    padding: 0 var(--container-padding);
    display: flex; flex-direction: column; gap: 24px;
  }
  .remote__image-wrap { border: 1px solid var(--color-border-solid); border-radius: 16px; padding: 12px; }
  .remote__image-inner {
    display: block;
    width: 100%; aspect-ratio: 800 / 534;
    background-color: var(--color-white-2);
    object-fit: cover; object-position: center;
    border-radius: 8px;
  }
  .remote__content { display: flex; flex-direction: column; gap: 16px; }
  .remote__title { font-size: 24px; font-weight: 500; color: var(--color-dark-blue); margin: 0; line-height: 1.3; }
  .remote__text { font-size: 16px; font-weight: 400; color: var(--color-text-dark); line-height: 1.6; margin: 0; }
  .remote__cta {
    display: inline-flex; align-items: center; gap: 8px;
    background-color: var(--color-blue); color: #ffffff;
    font-size: 14px; font-weight: 500; padding: 10px 24px;
    border-radius: 100px; text-decoration: none; height: 42px;
    align-self: flex-start; transition: opacity 0.2s ease;
  }
  .remote__cta:hover { opacity: 0.88; }
  @media (min-width: 768px) {
    .remote { padding: 80px 0; }
    .remote__container { flex-direction: row; align-items: center; gap: 80px; padding: 0; }
    .remote__image-wrap { flex: 0 0 424px; }
    .remote__content { flex: 1; gap: 24px; }
    .remote__title { font-size: 40px; line-height: 48px; }
    .remote__text { font-size: 18px; }
  }
</style>
