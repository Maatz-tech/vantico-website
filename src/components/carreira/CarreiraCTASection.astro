<section class="carreira-cta">
  <div class="carreira-cta__container">
    <div class="carreira-cta__box">
      <div class="carreira-cta__pattern" aria-hidden="true">
        <img src="/images/pentest/hero-pattern.svg" alt="" />
      </div>
      <div class="carreira-cta__content" data-animate>
        <h2 class="carreira-cta__title">Pronto para se juntar à nossa equipe?</h2>
        <p class="carreira-cta__text">Se não encontrar uma vaga disponível no botão abaixo, envie seu currículo para <strong>careers@vantico.com.br</strong> e registre seu interesse em fazer parte da Vantico.</p>
        <a href="https://vantico.com.br/vagas" class="carreira-cta__btn">
          Ver vagas
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
      </div>
    </div>
  </div>
</section>
<style>
  .carreira-cta { background-color: #ffffff; padding: 0 0 48px; }
  .carreira-cta__container { max-width: var(--container-max); margin: 0 auto; padding: 0 var(--container-padding); }
  .carreira-cta__box {
    position: relative; overflow: hidden;
    background-color: var(--color-white-2); border: 1px solid var(--color-border);
    display: flex; flex-direction: column; align-items: center;
    padding: 40px 24px; gap: 16px;
  }
  .carreira-cta__pattern {
    position: absolute; top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 1200px; height: 363px;
    pointer-events: none; z-index: 0; opacity: 0.6;
  }
  .carreira-cta__pattern img { width: 100%; height: 100%; }
  .carreira-cta__content {
    position: relative; z-index: 1;
    display: flex; flex-direction: column;
    align-items: center; gap: 16px;
    text-align: center; max-width: 700px;
  }
  .carreira-cta__title { font-size: 24px; font-weight: 500; color: var(--color-dark-blue); margin: 0; line-height: 1.3; }
  .carreira-cta__text { font-size: 16px; color: var(--color-text-dark); line-height: 1.6; margin: 0; }
  .carreira-cta__btn {
    display: inline-flex; align-items: center; gap: 8px;
    background-color: var(--color-blue); color: #ffffff;
    font-size: 14px; font-weight: 500; padding: 10px 24px;
    border-radius: 100px; text-decoration: none; height: 42px;
    transition: opacity 0.2s ease;
  }
  .carreira-cta__btn:hover { opacity: 0.88; }
  @media (min-width: 768px) {
    .carreira-cta { padding: 0 0 80px; }
    .carreira-cta__container { padding: 0; }
    .carreira-cta__box { padding: 48px; }
    .carreira-cta__title { font-size: 40px; line-height: 54px; }
  }
</style>
